import React from 'react';
const logo = require('../../public/logo.png');

export default () => (
  <div style={{ paddingTop: 100, height: '100vh', textAlign: 'center' }}>
    <img src={logo} alt="" />
  </div>
);
